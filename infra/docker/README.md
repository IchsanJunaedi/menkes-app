# Docker — Infrastructure

This directory contains Dockerfiles for each service.

## Files (added in Phase 0.3)

- `api.Dockerfile` — NestJS backend
- `web.Dockerfile` — Next.js frontend
- `nginx.Dockerfile` — NGINX reverse proxy (added in Phase 1.4)
